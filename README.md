# 🤖 BotForge - Создание Telegram-ботов

Telegram-бот для сбора заявок на создание ботов под ключ.

## 🚀 Функционал
- Опрос клиента по 6 шагам
- Сохранение в Google Sheets
- Уведомления администратора
- Редактирование данных
- Автоотмена через 10 минут неактивности

## 📁 Файлы проекта
- `bot.py` - основной код бота
- `requirements.txt` - зависимости Python
- `runtime.txt` - версия Python для Render
- `.python-version` - версия Python
- `.env.example` - шаблон переменных окружения
- `Procfile` - конфигурация для Render
- `.gitignore` - исключаемые файлы

## ⚙️ Установка на Render
1. Создайте Background Worker (не Web Service)
2. Добавьте переменные окружения из `.env.example`
3. Запустите деплой

## 🔧 Локальный запуск
```bash
# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл с переменными
cp .env.example .env
# Заполните .env своими данными

# Запустите бота
python bot.py
